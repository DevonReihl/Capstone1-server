DROP TABLE IF EXISTS team_members;

CREATE TABLE team_members(
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  full_name TEXT NOT NULL,
  gish_name TEXT NOT NULL,
  phone   VARCHAR(12)
);